{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm --prefix src ci --include=dev",
  "buildCommand": "npm --prefix src run build",
  "outputDirectory": "src/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
